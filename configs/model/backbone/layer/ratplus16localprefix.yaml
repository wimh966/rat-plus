_name_: ratplus16localprefix
d_model: ${model.backbone.d_model}
d_head: 64
num_head: ${eval:${.d_model} // ${.d_head}}
k_channel: ${.d_head}
bias: ${model.backbone.bias}
init: ${model.backbone.init}
ln: ${model.backbone.ln}
total_size: 64
chunk_size: 1
chunk_size1: 64
local_size: 0
prefix_size: 0
apply_re: true
mix_train: true
mix_train_mode: repeat
mix_train_ratio1: 0.5
mix_train_ratio2: 0.5
gate_bias: false